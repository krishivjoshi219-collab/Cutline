# react-native is the react-native-tvos fork (0.74.5-0). The prerelease
# suffix makes strict npm peer resolution reject it against ranges like
# ">=0.60 <1.0" and attempt a duplicate React Native install (ERESOLVE).
# The fork is API-compatible with stock 0.74, so relax peer checks.
legacy-peer-deps=true
