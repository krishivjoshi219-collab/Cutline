{
  "name": "cutline-firetv",
  "version": "0.1.0",
  "private": true,
  "description": "CUTLINE — time-budgeted viewing routes for Fire TV. Amazon App Dev Hackathon 2026.",
  "scripts": {
    "test": "node tests/solver.test.cjs",
    "test:strict": "node --unhandled-rejections=strict tests/solver.test.cjs",
    "serve": "python3 -m http.server 8080",
    "lint": "node -c js/solver.js && node -c js/player.js && node -c js/app.js && node -c js/scenes.js && node -c js/episode.js && echo OK"
  },
  "engines": { "node": ">=16" },
  "license": "MIT",
  "keywords": ["fire-tv", "hackathon", "video", "knapsack", "10-foot-ui"],
  "homepage": "https://example.com/cutline"
}
